<div class="modal-content">
    <div class="modal-header">
        <h5 class="modal-title">Supprimer Image Principale</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div class="modal-body">
        Tu es sur de vouloir faire cela?
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
        <form method="post" action="{{ path('admin.trick.delete.picture', {id: trickid}) }}" id="form_delete" name="delete_main_picture">
            <input type="hidden" name="_method" value="DELETE">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trickid)}}" id="token">
            <button class="btn btn-danger">Supprimer</button>
        </form>
    </div>
</div>